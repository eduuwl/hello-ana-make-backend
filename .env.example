# Connection string do Neon (Postgres). Pegue em https://console.neon.tech/ > seu projeto > Connection Details.
# Use a connection "pooled" para a app e a "direct" (sem -pooler) para rodar migrations, se o Neon separar as duas.
DATABASE_URL="postgresql://user:password@ep-xxxx-pooler.sa-east-1.aws.neon.tech/hello_ana_make?sslmode=require"
DIRECT_URL="postgresql://user:password@ep-xxxx.sa-east-1.aws.neon.tech/hello_ana_make?sslmode=require"

PORT=8000

# Segredos JWT — gere valores fortes e únicos em produção (ex.: openssl rand -hex 32)
JWT_ACCESS_SECRET="troque-este-segredo-de-access-token"
JWT_ACCESS_EXPIRES_IN="30m"
JWT_REFRESH_SECRET="troque-este-segredo-de-refresh-token"
JWT_REFRESH_EXPIRES_IN="30d"

# CORS — origem do frontend Next.js
CORS_ORIGIN="http://localhost:3000"

# URL pública do frontend, usada para montar links em e-mails (ex.: redefinição de senha)
FRONTEND_URL="http://localhost:3000"

# E-mails transacionais via Resend (https://resend.com/api-keys). Sem RESEND_API_KEY,
# o envio é pulado silenciosamente (só loga) — não bloqueia cadastro/pedido/reset de senha.
RESEND_API_KEY=""
MAIL_FROM="Hello Ana Make <onboarding@resend.dev>"

# Cupom de boas-vindas: agora mora em StoreSettings.signupPromotion (tabela store_settings,
# GET/PUT /api/v1/admin/settings) em vez de env var — editável pelo admin sem redeploy.

# Pagamentos (10-pagamentos.md). PAYMENT_WEBHOOK_SECRET é comparado ao header
# X-Webhook-Secret (mock) ou ao "asaas-access-token" que o Asaas manda em todo webhook —
# use o MESMO valor que você configurar como "Token de autenticação" no painel do Asaas.
PAYMENT_WEBHOOK_SECRET="troque-este-segredo-de-webhook"

# Gateway ativo ("mock" ou "asaas") e a API key ficam em StoreSettings.integrations
# (tabela store_settings), editáveis via PATCH /api/v1/admin/settings/integrations —
# não aqui. ASAAS_API_URL fica em env por ser infra (sandbox x produção), não um
# toggle de negócio; pegue a chave em https://asaas.com (produção) ou
# https://sandbox.asaas.com (testes), em Configurações > Integrações > API.
# DEFAULT do código = produção (https://api.asaas.com/v3). Só setar aqui pra usar o sandbox —
# e nesse caso a chave em StoreSettings.integrations também tem que ser a de sandbox
# (os dois ambientes do Asaas são separados; chave de um lado no endpoint do outro →
# "A chave de API informada não pertence a este ambiente").
# ASAAS_API_URL="https://api-sandbox.asaas.com/v3"

# Frete real via SuperFrete. Provider ativo ("mock" ou "superfrete") e o token ficam em
# StoreSettings.integrations (PATCH /api/v1/admin/settings/integrations), igual ao Asaas —
# não aqui. SUPERFRETE_API_URL fica em env por ser infra (sandbox x produção); pegue o token
# em https://superfrete.com (produção) ou https://sandbox.superfrete.com (testes). A
# cotação (calculator) usada aqui não gasta saldo — só a compra de etiqueta gastaria, e isso
# não foi implementado (só cotação). Sem provider "superfrete" configurado, o frete continua
# simulado (tabela fixa) normalmente.
# DEFAULT do código = produção (https://api.superfrete.com). Só setar aqui pra usar o sandbox
# (o token também tem que ser o de sandbox).
# SUPERFRETE_API_URL="https://sandbox.superfrete.com"

# Feed do Instagram na home (grid "Comunidade"). instagramAccessToken/instagramUserId ficam em
# StoreSettings.integrations (PATCH /api/v1/admin/settings/integrations) — gere em
# https://developers.facebook.com (crie um app > Instagram > API Setup with Instagram Login,
# conecte a conta Business/Creator @helloanamake, gere o token e pegue o Instagram User ID por
# lá). INSTAGRAM_APP_SECRET fica em env por ser segredo do app, não da loja — pegue no
# dashboard do mesmo app, em Configurações do App > Básico. Sem ela, o token colado é usado do
# jeito que está (funciona, mas expira em ~1h se for short-lived, sem renovação automática).
# Sem instagramUserId configurado, a seção "Comunidade" simplesmente não aparece na home.
INSTAGRAM_APP_SECRET=""
